#include "main.h"
/**
 * is_prime_number -prime number
 * @n: parameter
 * Return: return int val
 */
int is_prime_number(int n)
{
	float ret = (int)n;

	if ((int)ret % 2 != 0)
	{
		return (1);
	}
	return (0);
}
