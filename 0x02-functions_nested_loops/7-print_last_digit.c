#include "main.h"
/**
 * print_last_digit - return the last digit of a number
 * @n : parameter of function
 * Return: Always last 0
 */
int print_last_digit(int n)
{
int last;

last = n % 10;
return (last);
}
